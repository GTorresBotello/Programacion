#include <iostream>

using namespace std;

int main()
{
    char grado;
    int peso, bin, t, s, a;

    cout << "\nIngrese el valor para grado : ";
    cin >> grado;

    cout << grado;

    if (grado == 'A'){
        if (peso > 35){
            bin = 1;
            t = s + a;
        }
    }

    int suma = 0, conteo, fallo;

    if (conteo < 5){
        if (grado < 50){}
            fallo = fallo + 1;
     	}
     }

	return 0;
}
